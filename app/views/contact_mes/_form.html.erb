<%= form_for(@contact_me) do |f| %>
  <% if @contact_me.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_me.errors.count, "error") %> prohibited this contact_me from being saved:</h2>

      <ul>
      <% @contact_me.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
